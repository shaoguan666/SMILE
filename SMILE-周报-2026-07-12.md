# SMILE 项目周报（2026-07-03 至 2026-07-12）

## 一、本周工作概述

本周围绕 SMILE 的 AAAI-27 投稿准备，主要推进了论文代码与 checkpoint 证据核查、相关工作调研、论文正文压缩与补充材料迁移。
## 二、本周主要完成内容

### 1. 梳理 AAAI 投稿风险

1. 先处理 AAAI 主文页数和补充材料模板合规问题。
2. 再统一结果表、导出根目录和统计显著性证据链。
3. 最后压缩和调整叙事强度，尤其避免“所有任务均显著提升”一类过强表述。

当时审查确认的主要风险包括：`main1.pdf` 仍超过 AAAI 主文页数要求；`appendix.tex` 保留 IEEEtran 风格；结果表与脚本默认导出路径、协议口径存在漂移风险。这些问题被转化为后续正文压缩和补充材料迁移的依据。

### 2. 构建并运行 checkpoint 分支诊断

新增并验证了 checkpoint 诊断脚本：

- `analysis/diagnose_smile_checkpoint.py`

该脚本用于读取 `smart-smile-lean` 等模型 checkpoint，输出 density、CoMiss bias、FiLM、dual-head 等分支的参数范数、激活统计和 near-zero / active 判断。脚本设计上特别区分“参数证据”和“可信激活证据”：当 checkpoint 与当前源码结构不匹配时，只保留结构或参数层面的证据，不把 activation 结果写成可信结论。

随后对 `smart-smile-lean` 在 6 个数据集、3 个 seed 上的 18 个 `checkpoint-prc.pth` 进行了批量诊断，固定 `--split val --split-seed 42`。结果产物位于：

- `analysis/results/checkpoint_diagnostics_smart_smile_lean/summary_smart_smile_lean_prc_split42.csv`
- `analysis/results/checkpoint_diagnostics_smart_smile_lean/summary_smart_smile_lean_prc_split42.md`

汇总结论为：18 个 checkpoint 中 13 个具备可信激活证据，5 个因源码/checkpoint 张量形状漂移被跳过。可信运行中，density、CoMiss bias 和 FiLM 三条结构化路径均呈现非零激活；dual-head 在 `smart-smile-lean` 诊断中始终缺失。因此论文中可将该结果作为实现一致性检查放入补充材料，不应上升为新的性能实验结论，也不应把 dual-head 写入主模型叙事。

### 3. 相关工作与方法对比调研

本周完成了两类相关工作调研：

第一类是 AAAI 相关论文检索。根据 SMILE 当前定位，优先查找了临床不规则时间序列、missingness-aware learning、自监督表示学习、CDE/ODE/state-space 相关方法，并整理了若干与 SMILE 相关的 AAAI/OJS 链接，可用于后续 Related Work 扩展。

第二类是 PrimeNet 方法对比。调研后明确：PrimeNet 的 density 更接近不规则时间戳采样密度或预训练采样策略，而 SMILE 当前代码与论文中的 density 是由 mask 计算得到的局部观测频率，作为模型输入特征或结构化 missingness 表征使用。这个差异有助于后续论文中更准确地区分 SMILE 与已有 irregular time-series pretraining 方法。


